{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:admin": "pnpm --filter @workspace/admin run build",
    "build:mini-app": "pnpm --filter @workspace/mini-app run build",
    "build:api-server": "pnpm --filter @workspace/api-server run build",
    "build:railway": "pnpm run build:admin && pnpm run build:mini-app && pnpm run build:api-server",
    "start:api-server": "pnpm --filter @workspace/api-server run start",
    "start:railway": "node --enable-source-maps artifacts/api-server/dist/index.mjs",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "tsx": "catalog:"
  },
  "packageManager": "pnpm@9.0.0"
}
